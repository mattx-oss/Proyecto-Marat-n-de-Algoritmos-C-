#include <iostream>
using namespace std;
int main() {
    int n;
    cout << "Numero: ";
    cin >> n;
    if(n < 10)
        cout << "1 cifra";
    else if(n < 100)
        cout << "2 cifras";
    else
        cout << "3 o mas cifras";
    return 0;
}
