#include <iostream>
using namespace std;
int main() {
    int a;
    cout << "Anio: ";
    cin >> a;
    if((a % 4 == 0 && a % 100 != 0) || a % 400 == 0)
        cout << "Bisiesto";
    else
        cout << "No bisiesto";
    return 0;
}

