#include <iostream>
using namespace std;
int main() {
    int a,b,c,mayor;
    cout << "Tres numeros: ";
    cin >> a >> b >> c;
    mayor = a;
    if(b > mayor) mayor = b;
    if(c > mayor) mayor = c;
    cout << "Mayor: " << mayor;
    return 0;
}

